# MacOS 自用软件

## 浏览器

### Google Chrome

Google 出品的网页浏览器。

```bash
brew install google-chrome
```

> https://www.google.com/chrome

## 开发工具

### iTerm2

iTerm2 是 Terminal 的替代品，是 iTerm 的继承者。

```bash
brew install iterm2
```

> https://iterm2.com

### OrbStack

OrbStack 是一个轻量级、快速的 Docker 和 Linux 容器管理工具，可替代 Docker Desktop。

```bash
brew install orbstack
```

> https://orbstack.dev

### Docker

```bash
brew install docker
```

> https://www.docker.com/get-started

### Virtualbox

```bash
brew install virtualbox virtualbox-extension-pack
```

> https://www.virtualbox.org

### Vagrant

```bash
brew install vagrant vagrant-manager vagrant-vmware-utility
brew install vagrant-completion
```

> https://www.vagrantup.com

### Visual Studio Code

```bash
brew install visual-studio-code
```

> https://code.visualstudio.com/

### Trae

字节跳动推出的 AI IDE，基于 VS Code 定制，内置 AI 编码助手。

```bash
brew install trae
```

中文版：

```bash
brew install trae-cn
```

> https://www.trae.ai

### Kiro

AI 驱动的 IDE。

```bash
brew install kiro
```

> https://kiro.dev

### Azure Data Studio

微软开源的数据库桌面管理工具。该工具支持 SQL Server、Azure SQL DB 和 SQL DW 数据库，Windows 下的安装包仅不到 100 MB，还支持另外两大操作系统 macOS 和 Linux。

```bash
brew install azure-data-studio
```

> https://github.com/microsoft/azuredatastudio

### Beekeeper Studio

Beekeeper Studio 是一个跨平台的 SQL 编辑器和数据库管理器，可用于 Linux，Mac 和 Windows。适用于 MySQL，Postgres，SQLite，SQL Server 等的现代且易于使用的 SQL 客户端。

```bash
brew install beekeeper-studio
```

### DBeaver

适用于开发人员，数据库管理员，分析师和所有需要使用数据库的人员的免费的多平台数据库工具。支持所有流行的数据库。

```bash
brew install dbeaver-community
```

> https://dbeaver.io/

### Freelens

Freelens 是 Lens 的开源分支，用于管理 Kubernetes 集群的桌面应用程序。

```bash
brew install freelens
```

> https://github.com/freelensapp/freelens

### Headlamp

Kubernetes 集群管理的桌面 UI 工具。

```bash
brew install headlamp
```

> https://headlamp.dev

### Elasticvue

Elasticsearch 管理工具，提供可视化的索引管理和数据浏览功能。

```bash
brew install elasticvue
```

> https://github.com/cars10/elasticvue

### Act

让你在本地调试 GitHub Actions 脚本。GitHub Actions 是 GitHub 提供的仓库自动工作流程功能，用户可以利用 GitHub 提供的免费计算机资源轻松实现 CI/CD，还可以用来做很多有意思的事情。但是在编写 actions 脚本时，想调试脚本或得到运行结果，只能 push 到远程仓库等待运行结束，没有办法在本地调试和查看结果。有了 act 这个项目，就可以在本地轻松调试 actions 啦，是不是很棒。

```bash
brew install act
```

> https://github.com/nektos/act

### MySQL CLI

MyCLI 是 MySQL、MariaDB 和 Percona 的命令行界面，具有自动完成和语法高亮显示功能。

```bash
brew install mycli
```

> https://www.mycli.net

### Postgres CLI

Pgcli 是 Postgres 的命令行界面，具有自动完成和语法高亮显示功能。

```bash
brew install pgcli
```

> https://www.pgcli.com

### FileZilla

免费的 FTP 解决方案。

```bash
brew install filezilla
```

> https://filezilla-project.org

### SwitchHosts

Hosts 文件管理工具，支持快速切换不同的 Hosts 配置。

```bash
brew install switchhosts
```

> https://github.com/oldj/SwitchHosts

### OSS Browser

阿里云 OSS 官方桌面管理工具。

```bash
brew install oss-browser
```

> https://github.com/aliyun/oss-browser

### balenaEtcher

SD 卡和 USB 驱动器镜像烧录工具，常用于烧录系统镜像。

```bash
brew install balenaetcher
```

> https://etcher.balena.io

## AI 工具

### ChatGPT

OpenAI ChatGPT 桌面客户端。

```bash
brew install chatgpt
```

> https://chat.openai.com

### Claude

Anthropic Claude 桌面客户端。

```bash
brew install claude
```

> https://claude.ai

### Poe

Quora 推出的 AI 聊天聚合平台桌面客户端，可使用多种 AI 模型。

```bash
brew install poe
```

> https://poe.com

### Codex

OpenAI Codex 编码助手。

```bash
brew install codex
brew install codex-app
```

### OpenCode Desktop

AI 编码工具桌面客户端。

```bash
brew install opencode-desktop
```

## 系统辅助

### Lulu

免费开源的 macOS 防火墙软件。

```bash
brew install lulu
```

> https://github.com/objective-see/LuLu

### MacZip

免费开源的 macOS 解压缩软件。

```bash
brew install maczip
```

> https://ezip.awehunt.com

### Snipaste

Snipaste 是一个简单但强大的截图工具，也可以让你将截图贴回到屏幕上！下载并打开 Snipaste，按下 F1 来开始截图，再按 F3，截图就在桌面置顶显示了。就这么简单！

```bash
brew install snipaste
```

> https://snipaste.com

### alt-tab

Windows alt-tab on macOS。

```bash
brew install alt-tab
```

> https://github.com/lwouis/alt-tab-macos

### Rectangle

使用键盘快捷键和捕捉区域在 macOS 上移动和调整窗口大小。

```bash
brew install rectangle
```

> https://rectangleapp.com/

### Stats

macOS 系统状态监控工具，可显示 CPU、内存、磁盘、网络等信息在菜单栏。

```bash
brew install stats
```

> https://github.com/exelban/stats

### Maccy

适用于 macOS 的剪贴板管理器，只做一项工作 - 随时保留您的复制历史记录。

```bash
brew install maccy
```

> https://maccy.app

### Pearcleaner

macOS 应用卸载清理工具，可彻底清除应用残留文件。

```bash
brew install pearcleaner
```

> https://github.com/nicksinger/pearcleaner

### Latest

macOS 软件更新检查工具，可检测已安装应用的最新版本。

```bash
brew install latest
```

> https://github.com/mangerlahn/Latest

### WeType

微软出品的 macOS 中文输入法。

```bash
brew install wetype
```

> https://wetype.microsoft.com

### QuickLook Plugins

QuickLook 插件。

```bash
brew install qlcolorcode qlstephen qlmarkdown quicklook-json qlimagesize suspicious-package apparency quicklookase qlvideo quicklook-csv quicklook-pfm provisionql syntax-highlight webpquicklook
xattr -r ~/Library/QuickLook
xattr -d -r com.apple.quarantine ~/Library/QuickLook
```

> http://www.quicklookplugins.com/

## 网络通信

### 微信

```bash
brew install wechat
```

### 企业微信

```bash
brew install wechatwork
```

### 钉钉

```bash
brew install dingtalk
```

### 飞书

```bash
brew install feishu
```

### 腾讯会议

```bash
brew install tencent-meeting
```

### Telegram

跨平台即时通讯软件，注重安全和速度。

```bash
brew install telegram
```

> https://telegram.org

### Discord

跨平台语音、视频和文字通讯软件，广泛应用于游戏和社区。

```bash
brew install discord
```

> https://discord.com

## 云存储与同步

### OneDrive

微软 OneDrive 云存储桌面客户端。

```bash
brew install onedrive
```

### 阿里云盘

```bash
brew install adrive
```

### 百度网盘

```bash
brew install baidunetdisk
```

### 腾讯文档

```bash
brew install tencent-docs
```

## 科学上网

### Karing

基于 Clash 内核的代理工具。

```bash
brew install karing
```

> https://github.com/MeteorOpera/clashverge-rev

### Tailscale

基于 WireGuard 的零配置 VPN 组网工具。

```bash
brew install tailscale-app
```

> https://tailscale.com

### Tunnelblick

OpenVPN 客户端。

```bash
brew install tunnelblick
```

> https://tunnelblick.net

### OpenVPN Connect

```bash
brew install openvpn-connect
```

## 阅读写作

### Obsidian

强大的知识管理和笔记工具，支持 Markdown 和双向链接。

```bash
brew install obsidian
```

> https://obsidian.md

### Draw.io

draw.io 是一款免费的在线图表编辑工具, 可以用来编辑工作流, BPM, org charts, UML, ER图, 网络拓朴图等.

```bash
brew install drawio
```

> https://www.diagrams.net

### Bob

Bob 是一款 Mac 端翻译软件，支持划词翻译、截图翻译以及手动输入翻译。

```bash
brew install bob
```

> https://ripperhe.gitee.io/bob

### Typora

```bash
brew install typora
```

> https://www.typora.io/#download

### Mark Text

一款简洁优雅的 Markdown 编辑器，支持所见即所得。

```bash
brew install mark-text
```

> https://github.com/marktext/marktext

### NetNewsWire

macOS 和 iOS 上的免费开源 RSS 阅读器。

```bash
brew install netnewswire
```

> https://netnewswire.com

### Folo

下一代信息阅读器，支持 RSS 和社交媒体订阅。

```bash
brew install folo
```

> https://github.com/nicepkg/folo

### 语雀

蚂蚁集团推出的知识库和协作平台客户端。

```bash
brew install yuque
```

> https://www.yuque.com

### PlistEdit Pro

macOS plist 属性列表文件编辑工具。

```bash
brew install plistedit-pro
```

## 设计工具

### Figma

基于浏览器的协作式 UI 设计工具。

```bash
brew install figma
```

> https://www.figma.com

### PicGo

图床管理工具，支持多种图床服务，方便上传和管理图片。

```bash
brew install picgo
```

> https://github.com/Molunerfinn/PicGo

## 办公工具

### WPS Office

金山出品的办公软件套件，兼容 Microsoft Office 格式。

```bash
brew install wpsoffice-cn
```

> https://www.wps.cn

## 音乐视频

### Listen1

Listen1 可以搜索和播放来自网易云音乐，虾米，QQ音乐，酷狗音乐，酷我音乐，Bilibili，咪咕音乐网站的歌曲，让你的曲库更全面。

```bash
brew install listen1
```

> https://listen1.github.io/listen1

### IINA

一个现代的 macOS 媒体播放器。

```bash
brew install iina
```

> https://iina.io

### HandBrake

HandBrake 是一款免费开源的万能格式转换工具，这款软件能够支持目前市面上所有的视频格式文件。

```bash
brew install handbrake-app
```

> https://handbrake.fr

### Bilibili

Bilibili 桌面客户端。

```bash
brew install bilibili
```

> https://www.bilibili.com

## 远程控制

### 向日葵远控 (Awesun)

远程控制软件，支持跨平台远程桌面。

```bash
brew install awesun
```

### Windows App

微软远程桌面客户端，用于连接远程 Windows 计算机。

```bash
brew install windows-app
```

> https://apps.microsoft.com

## 黑苹果

### OpenCore Configuration

OpenCore Configurator 通过简化的图形界面为 OpenCore EFI 引导加载程序创建自定义配置文件。

```bash
brew install opencore-configurator
```

> http://mackie100projects.altervista.org
